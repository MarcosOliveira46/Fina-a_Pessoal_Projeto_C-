@model IEnumerable<Finance.Models.Receita>

@{
    ViewBag.Title = "Index";
}

<h2>Receita</h2>

<p>
    <button class="btn btn-success create" style="outline:none">Criar Receita <i class="glyphicon glyphicon-open"></i></button>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Receita_Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Data_Entrada)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Receita_Valor)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Receita_Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data_Entrada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Receita_Valor)
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id = item.ReceitaId }) |
        @Html.ActionLink("Details", "Details", new { id = item.ReceitaId }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.ReceitaId })*@
                @*<button class="btn btn-success details" data-id="@item.ReceitaId" style="outline:none"><i class="glyphicon glyphicon-file"></i></button>*@
                <button class="btn btn-danger delete" data-id="@item.ReceitaId" style="outline:none"><i class="glyphicon glyphicon-trash"></i></button>
                <button class="btn btn-primary edit" data-id="@item.ReceitaId" style="outline:none"><i class="glyphicon glyphicon-edit"></i></button>
            </td>
        </tr>
    }

</table>
<div id="modalRec" class="modal fade" role="dialog" />

@section scripts{
    <script>
        $(function () {
            $(".details").click(function () {
                var id = $(this).attr("data-id");
                /*alert(id);*/
                $("#modalRec").load("Receitas/Details?id=" + id, function () {//Receitas
                    $("#modalRec").modal();
                })
            });
            $(".edit").click(function () {
                var id = $(this).attr("data-id");
                /*alert(id);*/
                $("#modalRec").load("Receitas/Edit?id=" + id, function () {
                    $("#modalRec").modal();
                })
            });
            $(".delete").click(function () {
                var id = $(this).attr("data-id");
                /*alert(id);*/
                $("#modalRec").load("Receitas/Delete?id=" + id, function () {
                    $("#modalRec").modal();
                })
            });
           $(".create").click(function () {
               $("#modalRec").load("Receitas/Create", function () {
                   $("#modalRec").modal();
                })
            });
        })
    </script>
}